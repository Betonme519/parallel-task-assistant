# 隐私与发布检查

## 本地数据

任务、截图、运行缓存、导出备份与凭据都可能包含私人内容。`.gitignore` 排除常规数据目录、便携版、`.env`、密钥文件与本地验证记录。忽略规则无法撤回已经提交的文件，也无法保护任意位置的截图或手写秘密。

在 Flowline 目录运行：

```powershell
node scripts/privacy-check.cjs
git status --short --untracked-files=all
git diff --cached --stat
```

扫描同时覆盖已跟踪和未忽略的未跟踪文件，只报告路径、行号与风险类型，不打印匹配值。它是启发式检查，不等于完整安全审计：图片、任意格式的秘密、个人姓名和 Git 历史仍需另行核查。

## 发布源码

只提交确认过的源码、测试、依赖锁文件与公开文档。不要把整个工作目录拖到 GitHub 网页上传；这种方式不执行 Git 忽略规则。`docs/verification/` 保留为本机验证材料，默认不公开。

提交前检查 Git 作者姓名与邮箱是否符合公开意愿。演示截图应使用虚构任务，检查窗口标题、通知、头像、文件路径与其他可辨认信息。

如果密钥曾提交或公开，删除文件不足以恢复保密性，应先在服务商处撤销或轮换，再清理历史。本次工作不主动改写历史或操作服务商账户。

## 发布便携版

`npm run package` 使用独立的 `release-public/` 输出，仅纳入运行时代码、依赖和包元数据，不复制已有便携版数据。不要发布已经用于日常工作的运行目录。公开构建运行后生成的 `data/` 也属于私人数据。

## 网络边界

手动流程不需要 AI 密钥。截图识别会发送截图、补充说明与相关任务上下文；Windows 听写由操作系统处理。没有“自动清除截图里的敏感信息”功能。

专用凭据使用系统加密保存；工作区和 `.flowline` 备份中的任务文字、截图不是加密存储。

## 报告问题

公开 Issue 请只描述复现条件与脱敏后的错误信息。不要附带 API 密钥、完整工作区、私人截图或备份。

## 导出独立源码目录

在原始 Git 工作区的 Flowline 目录执行 `node scripts/export-source.cjs`。脚本先检查候选文件，再导出到 `open-source/Flowline/`，不复制 Git 历史、被忽略的数据或便携版。输出目录已存在时拒绝覆盖。

该目录可作为新 GitHub 仓库根目录。导出后不要在里面配置个人凭据或放入真实任务截图再整体上传。扫描与导出工具需要在 Git 工作区中运行。
