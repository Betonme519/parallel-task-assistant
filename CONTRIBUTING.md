# 贡献指南

欢迎提出问题、改进交互和提交代码。项目主要面向 Windows 上的个人多任务流程管理。

## 报告问题

请在 Issue 中提供：系统版本、显示缩放、启动方式、复现步骤、预期结果和实际结果。界面问题可以附虚构任务截图。请勿上传真实工作区、凭据、个人截图或完整备份。

功能建议请先说明具体工作场景、遇到的困难和期望行为。涉及云同步、外部平台接入或自动化执行的改动，建议先通过 Issue 讨论范围。

## 开发与提交

1. Fork 仓库并创建独立分支。
2. 执行 `npm ci`，用 `npm start` 检查界面。
3. 将修改集中在一个明确问题，避免混入无关重构。
4. 执行 `npm run check` 和 `npm test`；界面改动执行 `npm run test:e2e` 并补充适当的手动检查。
5. 执行 `npm run check:privacy` 并查看暂存内容。
6. 提交 PR，说明问题、修改后的行为、验证方法及已知限制。

业务规则优先放在 `src/core/`；系统调用、文件操作与凭据处理放在主进程。不要在渲染端新增任意文件系统或命令执行能力。依赖变更应同步锁文件，示例内容应使用虚构数据。

## 文档与图片

宣传图应以实际界面为依据，注明合成场景。真实截图也应使用隔离演示工作区。修改功能时同步维护 README、相关说明和测试入口。

## 许可

提交贡献前请确保你有权提供相关代码和素材。贡献将按项目现有的 MIT 许可证分发。交流请聚焦问题和证据，尊重不同使用场景。

## 提交说明

提交标题请用简短中文说明本次实际改动。不同模块的独立修改分别提交；不要为改变 GitHub 文件列表显示而制造空白或无意义变更。
